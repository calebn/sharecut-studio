/** Switch list virtualization on at this many rows… */
export const VIRTUALIZE_ON_ROWS = 200;
/** …and back off only below this many, so toggles near 200 do not flap layout. */
export const VIRTUALIZE_OFF_ROWS = 150;
