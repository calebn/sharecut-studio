/**
 * Timeline layout constants — keep in sync with CSS tokens in
 * styles/theme/tokens.css (--lane-height, --ruler-height, --marker-lane-height).
 */
export const LANE_HEIGHT = 72;
export const RULER_HEIGHT = 24;
/** Chapters + social + comments rows. */
export const MARKER_LANE_HEIGHT = 72;
